// Dashboard placeholder. The MVP pages land in Step 10.4.
export default function Home() {
  return (
    <section>
      <h1>Quality dashboard</h1>
      <p>
        The Test Commander web console renders the committed
        <code> .test-commander/ </code> workspace. It is read-only and
        proposal-only — it never changes the workspace or runs a command.
      </p>
    </section>
  );
}
