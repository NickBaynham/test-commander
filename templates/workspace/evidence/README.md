# Evidence

Artifacts from test runs. Screenshots committed; videos and traces git-ignored by default (opt-in lfs); logs committed.

> Populated by `/tc:run` (Phase 7).

_(empty until Phase 7 ships.)_
