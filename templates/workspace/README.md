# Test Commander Workspace

Canonical workspace template. `/tc:init` copies this tree into the consuming project's `.test-commander/` directory.

> Populated by `/tc:init` (Phase 1).

_(empty until Phase 1 ships.)_
