# Uploaded Documents

Drop raw source documents here (PDF, DOCX, MD, etc.). `/tc:learn-from-docs` walks this directory and builds the index.

> Populated by `/tc:learn-from-docs` (Phase 3).

_(empty until Phase 3 ships.)_
