# Documents

Source documents uploaded by the user (specs, PRDs, design notes). Subdirs hold the raw uploads; `index.md` is the searchable index.

> Populated by `/tc:init` (Phase 1).

_(empty until Phase 1 ships.)_
