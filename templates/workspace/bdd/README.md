# BDD

Behavior-Driven Development specs. `features/` holds Gherkin `.feature` files; `summaries/` holds per-feature Markdown summaries.

> Populated by `/tc:generate-bdd` (Phase 5).

_(empty until Phase 5 ships.)_
