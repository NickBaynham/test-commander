# Test Data

Test data lives here, never in test code. Subdirs: `seed/` baseline, `scenarios/` per-suite, `factories/` regenerable definitions. Regenerate via `/tc:generate-test-data`.

> Populated by `/tc:generate-test-data` (Phase 6).

_(empty until Phase 6 ships.)_
