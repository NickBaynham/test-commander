# Test Plan

The test plan and its requirement → coverage map.

- `test-plan.md` — the plan document (seeded once from the template, then human-owned).
- `coverage-map.md` — requirement → coverage table, regenerated on every update.

> Populated by `/tc:generate-test-plan` and kept current by `/tc:update-test-plan`.

_(empty until `/tc:generate-test-plan` runs.)_
