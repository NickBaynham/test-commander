# Assumptions

Cross-source assumption index. Populated by every `/tc:learn-from-*` command (Phase 3) under its own `## From <source>` section. Each entry is flagged distinctly from confirmed facts (no direct citation in the source). Sections from other commands are preserved across re-runs.

_(empty until Phase 3 ships.)_
