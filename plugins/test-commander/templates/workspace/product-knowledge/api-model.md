# API-Derived Model

Knowledge extracted from a recorded API-response playback at `documents/uploaded/recorded-api/responses.json` (configurable via `tc-knowledge.api.recorded-path`).

> Populated by `/tc:learn-from-api` (Phase 3). Re-runs overwrite this file byte-deterministically. v1 reaches only the recorded path; live mode is opt-in and refused under pytest.

_(empty until Phase 3 ships.)_
