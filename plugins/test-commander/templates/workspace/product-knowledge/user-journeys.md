# User Journeys

Cross-source user-journey index. Populated by every `/tc:learn-from-*` command (Phase 3) under its own `## From <source>` section. Each section is regenerated by its owning command; sections from other commands are preserved across re-runs.

_(empty until Phase 3 ships.)_
