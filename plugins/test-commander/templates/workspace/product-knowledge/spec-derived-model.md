# Spec-Derived Model

Knowledge extracted from OpenAPI specs and Postman v2.1 collections under `documents/uploaded/`.

> Populated by `/tc:learn-from-specs` (Phase 3). Re-runs overwrite this file byte-deterministically.

_(empty until Phase 3 ships.)_
