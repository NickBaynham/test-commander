# Code-Derived Model

Knowledge extracted from Python source under the configured root (default `documents/uploaded/code/`, configurable via `tc-knowledge.code.source-root`).

> Populated by `/tc:learn-from-code` (Phase 3). Re-runs overwrite this file byte-deterministically. v1 parses Python only; other languages are detected and flagged as `language-unsupported-in-v1` gaps.

_(empty until Phase 3 ships.)_
