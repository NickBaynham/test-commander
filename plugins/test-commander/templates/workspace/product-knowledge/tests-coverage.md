# Tests Coverage

Coverage signal extracted from existing test files: test-file counts, test-function inventory, and symbol-reference cross-check against `code-derived-model.md`.

> Populated by `/tc:learn-from-tests` (Phase 3). Re-runs overwrite this file byte-deterministically. v1 parses pytest-style Python (`test_*.py`, `*_test.py`); Playwright spec files (`*.spec.ts`) are detected and counted but not parsed.

_(empty until Phase 3 ships.)_
