# Quality Report History

Timestamped snapshots of `current-quality-report.md`. Committed by default (retention policy: keep forever per Q10).

> Populated by `/tc:report` (Phase 7).

_(empty until Phase 7 ships.)_
