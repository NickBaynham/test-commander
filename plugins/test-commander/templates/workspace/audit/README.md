# Audit

Append-only audit log of every action taken through the controlled execution pipeline. `actions.jsonl` is the canonical log; `approvals/` holds individual approval records.

> Populated by `Phase 10.5 controlled execution pipeline` (Phase 10.5).

_(empty until Phase 10.5 ships.)_
