# Requirement coverage

Requirement coverage: each requirement linked to the downstream artifact types it has. Generated by `/tc:diagram-coverage` from the requirements map; a requirement with no coverage stands alone.

```mermaid
flowchart TD
    req_req_<n>["REQ-<n>"]
    stage_test_ideas["Test ideas"]
    stage_bdd["BDD"]
    stage_automation["Automation"]
    req_req_<n> --> stage_bdd
```

> Sources: `traceability/requirements-map.md`
