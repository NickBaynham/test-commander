# Quality posture - infographic spec

The measured facts behind the brief, as a structured data block. Every value is a `[fact]` from the quality report; a metric the report does not state is omitted, never invented.

```yaml
requirements: <N>
latest_run: RUN-<id>
passed: <N>
failed: <N>
flaky: <N>
automated: <N>
automatable: <N>
risks: <N>
open_questions: <N>
evidence: <N>
```

> Sources: `quality-report/current-quality-report.md`
