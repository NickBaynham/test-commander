# Traceability chain

Full traceability chain: requirement to candidate scenario to test result. Generated by `/tc:diagram-traceability` from the test map; resolved results are shown and unresolved scenarios render `pending`.

```mermaid
flowchart LR
    req_req_<n>["REQ-<n>"]
    cs_cs_<n>["CS-<n>"]
    res_passed["Passed"]
    req_req_<n> --> cs_cs_<n>
    cs_cs_<n> --> res_passed
```

> Sources: `traceability/test-map.md`
