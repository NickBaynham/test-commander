# Test strategy

Test strategy: requirements feeding the automation decision buckets (automate / consider / manual). Generated by `/tc:diagram-test-strategy` from the requirements inventory and the automation plan.

```mermaid
flowchart TD
    requirements["Requirements (<n>)"]
    rank_automate["Automate"]
    sc_<slug>["<Scenario>"]
    requirements --> rank_automate
    rank_automate --> sc_<slug>
```

> Sources: `requirements/requirements-inventory.md`, `automation-plan/*.md`
