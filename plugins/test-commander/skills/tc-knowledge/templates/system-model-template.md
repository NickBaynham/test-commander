# System Model

Auto-generated by the `tc-knowledge` synthesizer. Regenerated by every `/tc:learn-from-*` command from the current state of the per-source model files and cross-cutting artifacts.

## Sources ingested

- **documents** - via `/tc:learn-from-docs`. See `documentation-model.md`.
- **specs** - via `/tc:learn-from-specs`. See `spec-derived-model.md`.
- **code** - via `/tc:learn-from-code`. See `code-derived-model.md`.
- **api** - via `/tc:learn-from-api`. See `api-model.md`.
- **tests** - via `/tc:learn-from-tests`. See `tests-coverage.md`.

Sources that have not yet been ingested are omitted; the section grows monotonically as the other `/tc:learn-from-*` commands run.

## Cross-source summary

### Entities (_N_)

- **_(EntityName)_** (from _sources_)
- From _documents_: _N_

See `entities.md` for the full per-source list.

### User journeys (_N_)

- **_(Journey title)_** (from _sources_)
- From _documents_: _N_

See `user-journeys.md` for the full per-source list.

### Business rules (_N_)

- From _documents_: _N_

See `business-rules.md` for the full per-source list.

### Assumptions (_N_)

- From _documents_: _N_

See `assumptions.md` for the full per-source list.
