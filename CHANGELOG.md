# Changelog

All notable changes to this project are documented in this file. The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Semantic versioning applies once a `0.1.0` release exists; until then, changes are grouped by phase.

## [Unreleased]

### Tooling / docs

- New one-line `CLAUDE.md` at the repo root containing `@AGENTS.md`. Claude Code's project-instruction loader walks up the directory tree looking for `CLAUDE.md` by name; without this file the per-project `AGENTS.md` was discoverable (a human or directed agent could find it via the README) but **not auto-loaded** into the session's system context. The one-line file uses Claude Code's `@<path>` import directive so `CLAUDE.md` loads `AGENTS.md` as project instructions, keeping `AGENTS.md` as the single canonical (vendor-neutral) source of truth and `CLAUDE.md` as the auto-load shim. Pattern matches `testingwithagents/Claude.md` which uses the same delegate. Verified: link checker covers 109 files (up from 108 — `CLAUDE.md` added with its `@` directive); the directive is not a Markdown link so it does not introduce link-check noise.

- New `AGENTS.md` at the repo root — the entry-point document an agent (Claude or any automated operator) reads at the start of every session before touching code. Without it, every new session had to re-discover the project's disciplines by reading the 1,900-line `planning/plan.md` end-to-end, reverse-engineering commit patterns from `git log`, and re-learning the TDD micro-cycle / verify chain / sign-off pattern. AGENTS.md captures: project identity in one paragraph (Test Commander as a domain-agnostic AI-assisted testing system); source-of-truth table pointing at the plan, CHANGELOG, README, docs, and the plugin skill directory; quick-reference one-liners for Decisions D1–D19; the seven Per-Phase Conventions with the full rule text (six per-phase deliverables, `claude` CLI for plugin ops, retiring prior-phase guards, SKILL.md surfaces shipped behavior, per-command page as single source of truth, customization-guide audit per D19, sub-step lesson capture); the strict 11-step TDD micro-cycle (write tests → red → implement → green → command page → methodology/template → SKILL.md update → make verify → lesson capture → CHANGELOG → commit); the helper-mirroring pattern that produced Step 2.3's 9/9-on-first-run result; the verify chain (ruff, pytest, verify_skills.py, check_links.py); commit conventions (HEREDOC multi-paragraph, `Co-Authored-By: Claude Opus 4.7` line per this repo's convention, per-sub-step commits, `plan:` prefix for plan-only commits); the six sub-sub-step phase sign-off pattern (cold-user walkthrough, per-step DoD audit, plan + CHANGELOG closing, doc final pass, test-first sign-off test with `>=` invariants, final DoD eval + annotated tag); the per-sub-step Definition of Done; a "What NOT to do" section catching the project's known anti-patterns (no `git add -A`, no exact-equals in sign-off tests, no template-stub-vs-generated confusion, no skip-not-overwrite on pure reports, no domain-specific vocabulary in shipped defaults, no deferral wording in shipped SKILL.md, no emojis); and a "When this orientation is wrong" closing — if drift is hit, the runbook is updated in the same PR. Cross-linked from `README.md` "For agents" section so human readers also discover it. The motivation is the same as the testingwithagents `docs/AGENT_BLOG_PUBLISHING.md` runbook from 2026-05-27: capture the operational knowledge once so the next session does not pay the rediscovery cost.

### Phase 3 — Project knowledge ingestion (in progress)

`tc-knowledge` is the third TC-owned skill; it will ship the five `/tc:learn-from-*` commands that read a consuming project's documents, OpenAPI/Postman specs, source code, recorded API responses, and existing tests and populate `<workspace>/product-knowledge/`. Phase-3 design decisions (folded in via the plan): universal cores plus `tc-knowledge:*` config extensions per D19; per-source model files plus namespaced cross-cutting sections so re-runs do not trample sibling commands' work; `system-model.md` regenerated by a shared synthesizer invoked from every helper; provenance citations are structural; gaps route to `requirements/open-questions.md`; Phase 3 does NOT write to `traceability/`; Python-only AST in v1 with TS/JS counted as `language-unsupported-in-v1`; recorded-mode API playback in tests with live mode opt-in and refused under the harness. Nine sub-steps in the plan; this entry tracks them as they land.

#### Added

- Step 3.6: `/tc:learn-from-tests` end-to-end — final `/tc:learn-from-*` command, closes the Phase-3 helper sweep. `plugins/test-commander/scripts/extract_knowledge_from_tests.py` walks the configured tests source root (default `<workspace>/documents/uploaded/tests/`, configurable via `tc-knowledge.tests.source-root`) and extracts pytest-style Python (`test_*.py`, `*_test.py`) via stdlib `ast` plus Playwright spec files (`*.spec.ts`) via regex counting (no TS parse in v1). Three positive dimensions: test-files (every recognized file with runner classification), test-functions (every `ast.FunctionDef`/`AsyncFunctionDef` starting with `test_`, with `<path>:<line>` provenance, captured via `ast.walk` so class-nested tests are reached), covered-symbols (union of `ast.Name.id` + `ast.Attribute.attr` references across every pytest test body — the aggregate symbol set used for the untested-function cross-check). Two gap signals routed to `<workspace>/requirements/open-questions.md` with the `[<kind>]` prefix (Step-3.4 convention) and deduplicated by `(source-id, question-text)`: `unsupported-test-runner` (every `*.spec.ts` file, always emitted — v1 detects + counts but cannot parse TypeScript) and `untested-function` (cross-check requires `code-derived-model.md` to be generated; mirrors Step-3.5's cross-helper-import pattern by importing `extract_knowledge_from_code` and calling its `aggregate()` to get parsed `Function` objects, then emitting the gap for every public function whose plain name does not appear in the covered-symbols aggregate). Writes four artifact families: (1) `<workspace>/product-knowledge/tests-coverage.md` (byte-deterministic overwrite — source-files table by runner + test-functions table with provenance + covered-symbols aggregate as backtick-fenced list + class-coverage breakdown vs `code-derived-model.md` + gap-signals section); (2) `## From tests` section overwrite in `entities.md` only — each class from `code-derived-model.md` annotated as `(confidence: covered)` when its name appears in the covered-symbols aggregate, `(confidence: uncovered)` otherwise (the section is rendered only when `code-derived-model.md` is generated; without code there are no classes to annotate); (3) `<workspace>/requirements/open-questions.md` appended with both gap kinds; (4) `<workspace>/product-knowledge/system-model.md` regenerated by the shared synthesizer. **Scope discipline**: `user-journeys.md`, `assumptions.md`, AND `business-rules.md` are ALL three NOT touched — tests encode no journeys, are confirmed facts not inferences, and express coverage rather than rules. Four explicit negative tests defend the contract (the most yet for a single sub-step). **Helper-mirroring pattern verified for the fifth time** (3.2 → 3.3 → 3.4 → 3.5 → 3.6): 23/23 GREEN on first run, no RED-to-GREEN cycle, same as Step 3.5. The Phase-3 skeleton is mature. **Workspace Layout update**: `tests-coverage.md` added as the 10th `product-knowledge/` artifact in both `planning/plan.md` Workspace Layout block and `docs/workspace-reference.md`. Workspace template stub authored at `templates/workspace/product-knowledge/tests-coverage.md`. The template-file count bumped from 63 to 64; two existing Phase-1 tests (`test_workspace_state.py::test_snapshot_after_fresh_init_has_zero_populated` and `test_phase_1_integration.py::test_full_phase_1_workflow`) asserted `== 63` exact-equality and broke. Per the Phase-2 Step-2.8 lesson ("every phase sign-off test that asserts a numeric cap, count, or version should assert `>=`, not `==`") both tests retrofitted with `>= 63` plus an `initial_count` capture for the re-init idempotency path; inline comment cites the Phase-2 lesson and names Phase-3 Step-3.6 as the bump. **New methodology**: `methodology/learning-from-tests.md` covers all 3 positive dimensions plus both gap signals with worked examples from the seeded fixture and Claude-judgment-layer paragraphs (distinguish "symbol in test body" from "symbol actually exercised", flag suspicious imports, identify Playwright surfaces unreachable in v1). **New template**: `tests-coverage-template.md`. **New command page**: `commands/learn-from-tests.md` with the per-command-page sections in order. **SKILL.md FINAL pass**: deferral wording removed for `/tc:learn-from-tests`; status section reframed from "X of Y shipped" to "Phase 3 helper sweep complete — all five `/tc:learn-from-*` commands plus the shared `synthesize_system_model.py` are shipped"; the "What to do when a slash command fires" section enumerates per-command judgment-layer focus for all five commands; final `grep -niE "behavior arrives in|coming in phase|placeholder|until.*ships"` returns no hits anywhere in `tc-knowledge/SKILL.md`. Pre-flight `tests/test_learn_from_tests.py` (23 tests, test-first) covers: uninitialized refused, no-tests writes stub + regenerates system-model reflecting no-source state, seeded `tests/` extracts 2 pytest files + 4 test functions + covered-symbols + 1 Playwright file, `[unsupported-test-runner]` (web.spec.ts) ALWAYS routes to open-questions, `[untested-function]` (upload_file) routes ONLY after `/tc:learn-from-code` ran, cross-check skipped without code-derived-model, `## From tests` populates `entities.md` (covered + uncovered classes with confidence labels) BUT NOT `user-journeys.md`, `assumptions.md`, OR `business-rules.md` (four negative assertions), `## From documents` + `## From specs` + `## From code` + `## From api` ALL preserved when 3.6 runs after the other four helpers, byte-identical re-run across all outputs, line-stable open-questions, `source-root` extension points the walker at a custom directory, synthesizer reflects tests ingestion. TDD trace: 23 RED on first run; 23/23 GREEN after first cut of helper — no bug-fix cycle, mirroring the Step-3.5 outcome. Two unrelated Phase-1 tests broke due to the workspace-template addition; retrofitted with `>=` per the Phase-2 lesson. **Lint cleanup**: ruff initially flagged 1 E501 (long Playwright table-row f-string) — extracted `runner = "playwright (counted, not parsed)"` to a variable; 1 E501 (long isinstance line) — captured `is_def = isinstance(...)` as a variable matching the Step-3.4 pattern. 311-test suite green (288 baseline + 23 new + 2 unrelated Phase-1 retrofits stayed green); lint clean; link checker covers 136 files (up from 133 — new methodology + template + command page; +1 from prior count was the api-model.md stub update in Step 3.5). `verify_skills.py` reports `tc-core PRESENT (phase 1)`, `tc-requirements PRESENT (phase 2)`, `tc-knowledge UNEXPECTED (phase 3) — ahead of schedule` (cap bump still in 3.8). `planning/plan.md` Phase 3 Lessons learned updated with seven Step-3.6 entries (helper-mirroring fifth datapoint and pattern fully reinforced; workspace template addition tripped Phase-1 exact-equality assertions with Phase-2-lesson retrofit; cross-helper import doubles as ergonomic test setup; SKILL.md final deferral-wording grep is fast and catches stragglers; entities.md confidence-annotation convention `(confidence: <label>)`; Phase 3 helper sweep closes cleanly with 311 tests + 136 files; remaining sub-steps shape — 3.7 docs pass, 3.8 testing finalization with cap bump, 3.9 sign-off).
- Step 3.5: `/tc:learn-from-api` end-to-end. `plugins/test-commander/scripts/extract_knowledge_from_api.py` reads a recorded-response playback file (default `<workspace>/documents/uploaded/recorded-api/responses.json`, configurable via `tc-knowledge.api.recorded-path`) and parses each `{method, path, status, headers, body}` entry into a `Recording` dataclass with status-family classification (`2xx` / `3xx` / `4xx` / `5xx`), top-level response-body keys (JSON objects directly; arrays of objects use the first element's keys), an `Authorization`-header presence flag, and a `<file>:<index>` provenance citation. Three positive dimensions: live-endpoints (every playback entry); response-shapes (sorted top-level JSON keys per body); auth-required (inferred when the request carried an `Authorization` header OR the response is 401/403 without one). Two gap signals routed to open-questions with the `[<kind>]` prefix (Step-3.4 convention) and deduplicated by `(source-id, question-text)` — both require `spec-derived-model.md` to be generated (cross-check skipped without spec): `unspecified-endpoint` (recorded `(method, path)` not in the spec) and `mismatched-status` (recorded status not in the spec endpoint's declared status set, when the spec declares at least one status — silent when spec declares none, to avoid emitting redundant gaps alongside the spec-side `unspecified-status`). **Cross-helper import**: 3.5 imports `extract_knowledge_from_specs` and reuses Step 3.3's `aggregate()` to get parsed `Endpoint` objects with their declared statuses; this avoids duplicating OpenAPI / Postman parsing. Required a small backward-compatible refactor of 3.3's `Endpoint` dataclass: added `statuses: tuple[str, ...] = ()` field populated from `paths.<path>.<method>.responses` keys during OpenAPI extraction. 3.3's `render_spec_model` does not surface statuses, so 3.3's 21-test suite stayed green (no rendered-output change). **Live mode opt-in and refused under pytest**: `tc-knowledge.api.mode: live` (vs default `recorded`) attempts live HTTP probing in v2; v1 stops at refusal. The refusal logic detects pytest via `os.environ.get("PYTEST_CURRENT_TEST")` (pytest sets this env var for every test) and exits 2 with a `live mode refused under pytest` error before constructing any HTTP request. Outside pytest, live mode exits 2 with a `not implemented in v1` error. No network calls leak from the test suite. **Cross-cutting contributions**: `## From api` populates `entities.md` (resources confirmed reachable, grouped by first non-templated URL segment using the same logic as 3.3's spec resource detection) and `business-rules.md` (one rule per auth-required endpoint). `user-journeys.md` and `assumptions.md` are NOT touched — API recordings encode no journeys and recordings are confirmed facts not inferences. Two explicit negative tests defend the scope contract. **Fixture realignment**: `tests/fixtures/seeded-sample-project/recorded-api/responses.json` updated to move the `mismatched-status` marker from `POST /workspaces/{id}/assets returning 500` (where the spec has no `responses` declared, so the mismatch had nothing to compare against) to `DELETE /sessions/{id} returning 500` (the spec declares only 204 for this endpoint, so 500 is a clean mismatch). `POST /workspaces/{id}/assets` changed to 201 success. The 3.1 scaffold test still passes (the `mismatched-status` knowledge marker remains in the fixture); 3.2/3.3/3.4 tests stayed green (none pin status values). **New methodology**: `methodology/learning-from-api.md` covers all 3 positive dimensions plus both gap signals with worked examples from the seeded fixture and Claude-judgment-layer paragraphs (correlate observed shape with spec schemas, flag shapes including sensitive fields, identify endpoints that should require auth but don't, explain auth flows in plain language). **New template**: `api-model-template.md`. **New command page**: `commands/learn-from-api.md` with the per-command-page sections in order. **Workspace template stub refreshed**: `templates/workspace/product-knowledge/api-model.md` describes the recorded-path default, the live-mode opt-in, and the pytest-refusal contract. **SKILL.md updated**: deferral wording removed for `/tc:learn-from-api`; status line lists 3.2-3.5 as shipped; per-command description includes the shipped behavior summary and the `Run:` example. Pre-flight `tests/test_learn_from_api.py` (23 tests, test-first) covers: uninitialized refused, no-recorded-file writes stub + regenerates system-model.md reflecting no-source state, seeded responses.json extracts all 7 entries with method+path+status provenance, response shapes captured (account_id / display_name / id / etc.), `[unspecified-endpoint]` (GET /accounts/me) routes to open-questions ONLY after `/tc:learn-from-specs` has populated spec-derived-model.md, `[mismatched-status]` (DELETE /sessions/{id} 500 vs spec's 204) routes to open-questions after specs ran, both gap signals skipped without spec model, auth-required dimension surfaced for endpoints with Authorization header, `## From api` populates `entities.md` (resources observed) and `business-rules.md` (auth rules) but explicitly NOT `user-journeys.md` or `assumptions.md`, `## From documents` (3.2) + `## From specs` (3.3) + `## From code` (3.4) preserved when 3.5 runs after all three, byte-identical re-run for api-model + cross-cutting + system-model, line-stable open-questions, `recorded-path` extension points the walker at a custom playback file, `mode: live` refused under pytest with `live mode refused` in stderr, synthesizer reflects api ingestion. TDD trace: 23 RED on first run; **23 GREEN after first cut of helper** — no bug-fix cycle needed. This is the first Phase-3 sub-step to land 100% GREEN on first run; attributable to the now-well-trodden helper-mirroring skeleton (3.2→3.3→3.4→3.5) plus the lessons codified from prior sub-steps (kind-prefix on open-questions, byte-deterministic outputs, generator-marker check for upstream artifacts, explicit cross-cutting scope). **Lint cleanup**: ruff initially flagged 2 trivial f-string-without-interpolation warnings; auto-fixed. 288-test suite green (265 baseline + 23 new); lint clean; link checker covers 132 files (up from 129 — new methodology + template + command page). `verify_skills.py` reports `tc-core PRESENT (phase 1)`, `tc-requirements PRESENT (phase 2)`, `tc-knowledge UNEXPECTED (phase 3) — ahead of schedule` (cap bump still in 3.8). `planning/plan.md` Phase 3 Lessons learned updated with eight Step-3.5 entries (helper-mirroring 23/23 GREEN on first run is the fourth datapoint, cross-helper import via dataclass-field extension, fixture realignment for cross-source semantics second occurrence, live-mode refusal via PYTEST_CURRENT_TEST env var pattern, Authorization-header auth-required inference, stable cross-cutting section order across all four sources, helper-level + renderer-level defense-in-depth on empty sections, tc-knowledge.api schema lands but customization-guide update remains deferred to 3.7). Smoke run against the seeded fixture: 7 recordings parsed (POST /sessions 201, DELETE /sessions/{id} 500, GET /accounts/{id} 200, GET /workspaces 200 with auth, POST /workspaces/{id}/assets 201 with auth, GET /workspaces/{id}/assets 200 with auth, GET /accounts/me 200 with auth); 3 auth-required endpoints (GET /workspaces, POST + GET /workspaces/{id}/assets, GET /accounts/me); 2 gap signals (unspecified-endpoint for GET /accounts/me + mismatched-status for DELETE /sessions/{id}); entities.md `## From api` lists 3 resources (sessions, accounts, workspaces); business-rules.md `## From api` lists 4 auth rules.
- Step 3.4: `/tc:learn-from-code` end-to-end. `plugins/test-commander/scripts/extract_knowledge_from_code.py` walks the configured source root (default `<workspace>/documents/uploaded/code/`, configurable via `tc-knowledge.code.source-root` relative to the workspace) and uses stdlib `ast` to extract Python modules, classes, functions, docstrings, and decorators with `<path>:<line>` provenance. Non-Python files in the unsupported-extension set (`.ts`, `.tsx`, `.js`, `.jsx`, `.go`, `.java`, `.rb`) are detected by extension and flagged as `language-unsupported-in-v1` gaps — never silently ignored. Five positive dimensions: modules (every `*.py` file successfully parsed by `ast.parse`); classes (every `ast.ClassDef` with name + line + docstring + bases + attributes from `__init__` `ast.Assign` and `ast.AnnAssign` walks; public methods also surfaced as `ClassName.method` functions); functions (`ast.FunctionDef` + `ast.AsyncFunctionDef` with decorators rendered back to source-like strings — `@validator`, `@app.get`, `@router.get(...)` for `ast.Call`); docstrings (`ast.get_docstring()` per module/class/function, first non-empty line surfaced in the rendered model); decorators (captured per function, available as cross-check anchors). Three gap signals: `undocumented-function` (public name not starting with `_`, including `Class.method` form where method also does not start with `_`, with no docstring); `language-unsupported-in-v1` (file with unsupported extension); `unimplemented-endpoint` (cross-check: when `spec-derived-model.md` carries the `Auto-generated by /tc:learn-from-specs` marker AND no empty-run sentinel, parse its endpoints table for `(method, path, operationId)` triples; emit gap for every endpoint whose `operationId` does not equal any function name — both plain `name` and the trailing segment of `Class.method` are considered). Cross-check is order-independent: code-without-specs produces zero `unimplemented-endpoint`; running specs later then re-running code lands them. Writes four artifact families: (1) `<workspace>/product-knowledge/code-derived-model.md` (byte-deterministic overwrite — source modules table + classes table with attributes column + classes one-liner-docstring bullets + functions table with decorators column + functions one-liner-docstring bullets + unsupported-files table + gap-signals section); (2) `## From code` section overwrite in `entities.md` (classes as domain-entity candidates with bolded-leading-name bullets `- **ClassName** (path:line) - docstring summary`); the same section is reserved in `business-rules.md` for module-level constants carrying explicit rule docstrings but is empty in v1 (the seeded fixture has no such constants; the renderer omits empty sections); (3) `<workspace>/requirements/open-questions.md` appended with gap-signal questions, deduplicated by `(source-id, question-text)` (source-id `tc-knowledge/learn-from-code`); **the question text now prefixes the gap kind** (`[undocumented-function] Public function 'upload_file' ...`) so grep-by-kind on the open-questions file works uniformly; (4) `<workspace>/product-knowledge/system-model.md` regenerated by the shared synthesizer. **Scope discipline**: `user-journeys.md` and `assumptions.md` are NOT touched — code declares no journeys and code facts are confirmed not inferences; two explicit negative tests defend this contract. **Helper-mirroring pattern verified for the third time** (3.2 → 3.3 → 3.4): the helper was authored by copy-renaming `extract_knowledge_from_specs.py`'s skeleton; result was 20/23 GREEN on first run with 3 RED of the same root cause (open-question text didn't include the gap kind), fixed by prefixing the kind in `append_open_questions`. **Fixture realignment**: updated `tests/fixtures/seeded-sample-project/specs/openapi.yaml` operationIds to match Python function names (`create_session→sign_in`, `destroy_session→sign_out`, `upload_asset→upload_file`) so the spec-vs-code cross-check produces exactly one `unimplemented-endpoint` gap (`GET /workspaces` with operationId `list_workspaces`, the seeded marker); added `src/app/api/accounts.py` with `get_account()` so `GET /accounts/{id}` (operationId `get_account`) resolves to a real function. 3.3's tests didn't pin specific operationId values, so they stayed green after the realignment. **New methodology**: `methodology/learning-from-code.md` covers all 5 positive dimensions plus all 3 gap signals with worked examples from the seeded fixture and Claude-judgment-layer paragraphs (entity vs implementation-detail classification, internal-helper vs public-API distinction, signature-implied failure modes). **New template**: `code-derived-model-template.md` with full output structure. **New command page**: `commands/learn-from-code.md` with all per-command-page sections in order. **Workspace template stub refreshed**: `templates/workspace/product-knowledge/code-derived-model.md` describes the configurable source root and v1's Python-only parsing. **SKILL.md updated**: deferral wording removed for `/tc:learn-from-code`; status line lists 3.2, 3.3, and 3.4 as shipped; per-command description includes the shipped behavior summary and the `Run:` example. Pre-flight `tests/test_learn_from_code.py` (23 tests, test-first) covers: uninitialized refused, no-code writes stub model + regenerates system-model.md reflecting no-source state, seeded `src/` extracts all classes (Account, Workspace) and all 9 public functions (sign_in, sign_out, upload_file, list_assets, get_account, plus the Class.method form for Account.is_admin and Workspace.add_asset, plus is_non_empty_string and is_positive_int) with `<path>:<line>` provenance, docstring first-line surfaced for documented functions, `undocumented-function` (upload_file) routes to open-questions with the `[undocumented-function]` kind prefix, `language-unsupported-in-v1` (web/app.ts) routes to open-questions, `unimplemented-endpoint` (GET /workspaces with operationId list_workspaces) routes to open-questions ONLY after `/tc:learn-from-specs` has populated spec-derived-model.md, cross-check skipped without spec model, `## From code` populates entities.md but NOT user-journeys.md or assumptions.md, `## From documents` and `## From specs` preserved when 3.4 runs after 3.2+3.3, byte-identical re-run for code-derived-model + cross-cutting + system-model, line-stable open-questions, `source-root` extension points the walker at `<workspace>/src/`, `ignored-paths` extension excludes matching subdirs (migrations), synthesizer reflects code ingestion. TDD trace: 23 RED on first run; 20 GREEN after first cut; 3 RED remaining (open-question text format); fixed by prefixing gap kind; 23/23 GREEN. **Lint cleanup**: ruff initially flagged three E501s; condensed the AST isinstance checks into multi-line forms (`is_init_def = isinstance(...)` captured, then checked) and merged a nested elif/if into a single AnnAssign `and`-chain. 265-test suite green (242 baseline + 23 new); lint clean; link checker covers 129 files (up from 126 — new methodology + template + command page). `verify_skills.py` reports `tc-core PRESENT (phase 1)`, `tc-requirements PRESENT (phase 2)`, `tc-knowledge UNEXPECTED (phase 3) — ahead of schedule` (cap bump still in 3.8). `planning/plan.md` Phase 3 Lessons learned updated with eight Step-3.4 entries (fixture realignment for cross-source matching, open-question kind prefix as new convention, generator-marker check pattern recurs for the third time, AST attribute extraction must handle both `Assign` and `AnnAssign`, stdlib-first dependency policy verified, helper-mirroring third datapoint, `enabled-languages: []` short-circuit, `source-root` workspace-relative resolution semantics). Smoke run against the seeded fixture: 2 classes (Account with 3 attributes, Workspace with 4 attributes including assets list), 13 functions (sign_in, sign_out, upload_file (no docstring → gap), list_assets, get_account, Account.is_admin, Workspace.add_asset, is_non_empty_string, is_positive_int, plus the workspace `__init__` constructors which are private), 1 TypeScript file (web/app.ts) flagged as language-unsupported-in-v1, 1 unimplemented-endpoint gap (GET /workspaces); entities.md `## From code` lists Account and Workspace with their docstring summaries.
- Step 3.3: `/tc:learn-from-specs` end-to-end. `plugins/test-commander/scripts/extract_knowledge_from_specs.py` auto-detects OpenAPI 3 (`.yaml`, `.yml`, `.openapi.yaml`, `.openapi.yml`, `.openapi.json`) and Postman v2.1 (`*.postman_collection.json`) specs anywhere under `<workspace>/documents/uploaded/`. OpenAPI extractor walks `paths.<path>.<method>` triples for endpoints, `components.schemas` for schemas, and `components.securitySchemes` for auth schemes. Postman extractor walks `item[*].request` recursively (descends through nested folders via `item[*].item`), captures `request.method` + URL path (handling all three Postman URL shapes — `url.raw` string, `url.path` segment array, fully-qualified URL with `{{base_url}}` template prefix via `_strip_postman_variables`), derives `<request-label>Body` schemas from `request.body.raw` JSON top-level keys, and surfaces distinct `request.auth.type` values as auth schemes. Resolves `<path>:<line>` provenance by string-scanning the source text for each finding's structural marker (`<path>:`, `<method>:`, `<schema-name>:`, etc.). Two gap signals detected per the Step-3.3 partition table: `unspecified-status` (endpoint declares no `responses` keys OR only `default`) and `schema-without-type` (schema entry missing both `type` and `$ref`). Writes four artifact families: (1) `<workspace>/product-knowledge/spec-derived-model.md` (byte-deterministic overwrite — source-specs table + endpoints table with combined `METHOD /path` cells + schemas table with type + $ref columns + auth-schemes table + gap-signals section); (2) `## From specs` section overwrite in `entities.md` (endpoints grouped by HTTP-path resource — first non-templated URL segment — with method list and first citation) and `business-rules.md` (each auth scheme as a rule); (3) `<workspace>/requirements/open-questions.md` appended with gap-signal questions, deduplicated by `(source-id, question-text)` pair (source-id `tc-knowledge/learn-from-specs`); (4) `<workspace>/product-knowledge/system-model.md` regenerated by the shared synthesizer. **Scope discipline**: `user-journeys.md` and `assumptions.md` are NOT touched — specs declare no journeys and specs are confirmed facts (not inferences); two explicit negative tests defend this contract against regression. **Helper-mirroring pattern verified** (the Phase-2 Step-2.3 prediction): the helper was authored by copy-renaming `extract_knowledge_from_docs.py`'s skeleton and adapting only source-format detection + per-format extractors + gap detectors + cross-cutting scope; result was 19/21 GREEN on first run with one cosmetic bug (table cells separated `POST` from `/sessions` so the test's `"POST /sessions"` substring assertion missed; fixed by merging method + path into a single cell). **New dependency**: `pyyaml>=6.0` added to `pyproject.toml` `[project.dependencies]`. First non-stdlib dependency; pure-Python, canonical YAML library. Future Phase-3 sub-steps and later phases (Phase 6 test data, Phase 10.5 policy) will reuse it. Project policy is now "stdlib-first; add a canonical pure-Python dep when the alternative is a custom parser for a complex grammar". **New methodology**: `methodology/learning-from-specs.md` covers all 3 positive dimensions (endpoints, schemas, auth-schemes) with worked examples drawn from the seeded fixture, plus the 2 gap signals, plus Claude-judgment-layer paragraphs (which endpoints are core vs auxiliary, which schemas are entities vs parameter sets, what auth credentials testers need). **New template**: `spec-derived-model-template.md`. **New command page**: `commands/learn-from-specs.md` with the per-command-page sections in order. **Workspace template stub updated**: `templates/workspace/product-knowledge/spec-derived-model.md` describes what 3.3 writes. **SKILL.md updated**: deferral wording removed for `/tc:learn-from-specs`; status line lists both 3.2 and 3.3 as shipped; per-command description includes the shipped behavior summary and the `Run:` example. Pre-flight `tests/test_learn_from_specs.py` (21 tests, test-first) covers: uninitialized workspace refused, no-spec writes stub model + regenerates system-model.md reflecting no-source state, seeded `openapi.yaml` extracts all 6 endpoints + 6 schemas + bearerAuth scheme with `<path>:<line>` provenance, `unspecified-status` (POST /workspaces/{id}/assets) routes to open-questions, `schema-without-type` (AssetUpload) routes to open-questions, `## From specs` populates `entities.md` (endpoints as resources) AND `business-rules.md` (bearerAuth) but explicitly does NOT populate `user-journeys.md` or `assumptions.md`, `## From documents` sections from Step 3.2 preserved when 3.3 runs after 3.2, byte-identical re-run for spec-derived-model + cross-cutting + system-model, line-stable open-questions on re-run, synthetic Postman v2.1 collection auto-detected and parsed (POST /sessions, GET /workspaces with bearer auth), synthesizer reflects specs ingestion in the cross-source summary. TDD trace: 21 RED on first run; 19 GREEN after first cut of helper; 2 RED remaining (both same cosmetic issue — Markdown table column separation broke `METHOD PATH` substring assertion); merged method + path into a single table cell; 21/21 GREEN. **Lint cleanup**: ruff initially flagged three issues — import-order auto-fixed; one E501 (operationId ternary on one line) split into a two-line form (`op_id_raw` captured first, then type-check ternary); one SIM108 nested-if-else condensed to ternary for the Postman URL extraction. 242-test suite green (221 baseline + 21 new); lint clean; link checker covers 126 files (up from 123 — new methodology + template + command page). `verify_skills.py` reports `tc-core PRESENT (phase 1)`, `tc-requirements PRESENT (phase 2)`, `tc-knowledge UNEXPECTED (phase 3) — ahead of schedule` (cap bump still lives in Step 3.8). `planning/plan.md` Phase 3 Lessons learned updated with six Step-3.3 entries (helper-mirroring prediction verified; PyYAML as first non-stdlib dep; table-cell substring assertion shape; OpenAPI heterogeneous-field type-narrowing pattern; explicit negative cross-cutting tests defend section-overwrite scope; Postman URL extraction handles three shapes). Smoke run against the seeded openapi.yaml: 6 endpoints parsed (POST /sessions, DELETE /sessions/{id}, GET /accounts/{id}, GET /workspaces, POST /workspaces/{id}/assets, GET /workspaces/{id}/assets), 6 schemas (SignInRequest, Session, Account, Workspace, Asset, AssetUpload), 1 auth scheme (bearerAuth: http + bearer + JWT), 2 gap signals routed to open-questions (unspecified-status for POST /workspaces/{id}/assets which has no `responses:` block in the fixture, schema-without-type for AssetUpload which lacks `type:` and `$ref:`), entities.md `## From specs` has 5 grouped resources (sessions, accounts, workspaces from the URL paths), business-rules.md `## From specs` has the bearerAuth rule.
- Step 3.2: `/tc:learn-from-docs` end-to-end plus the shared `synthesize_system_model.py` synthesizer. `plugins/test-commander/scripts/extract_knowledge_from_docs.py` reads every `*.md` under `<workspace>/documents/uploaded/` that does NOT contain a `REQ-\d+` token (Phase-2 inverse filter), applies five universal-core extractors per the Step-3.2 partition table: `entities` (capitalized noun phrases under headings containing `entit`/`model`/`noun`/`glossary` — both bullet-with-bolded-name and table-row shapes), `terms` (definition-list `Term\n: definition` plus glossary-heading table rows), `user-journeys` (numbered or bulleted lists under headings containing `journey`/`flow`/`walkthrough`/`scenario`, with same-or-shallower-level termination so steps under H2 child sections remain in scope of an H1 journey heading), `business-rules` (RFC-2119 modal sentences outside journey ranges), `assumptions` (sentences containing assumption markers `assume`/`expected`/`presumed`/`likely`, tagged "no direct citation"). Detects two gap signals: `undefined-term` (capitalized phrase appearing in >=2 documents OR bolded in prose, never the subject of a glossary/definition-list entry) and `contradictory-rule` (rules sharing a subject anchor where one carries a negation marker `not`/`never`/`without` and the other does not). Writes four artifact families: (1) `<workspace>/product-knowledge/documentation-model.md` (byte-deterministic overwrite — full report: sources table + entities table + terms table + per-journey detail + rules table + assumptions list with "no direct citation" flag + gap-signals section); (2) `## From documents` section overwrite in each of `entities.md`, `user-journeys.md`, `business-rules.md`, `assumptions.md`, preserving every other source's `## From <other-source>` section verbatim (stable section order: `documents` → `specs` → `code` → `api` → `tests`); (3) `<workspace>/requirements/open-questions.md` appended with gap-signal questions, deduplicated by `(source-id, question-text)` pair using the Phase-2 contract; (4) `<workspace>/product-knowledge/system-model.md` regenerated by the shared synthesizer. **Shared synthesizer** `synthesize_system_model.py` reads the current state of every per-source model plus every cross-cutting artifact and rewrites `system-model.md` byte-deterministically — sources whose model still carries the workspace-template stub marker `_(empty until Phase 3 ships.)_` OR any helper-emitted empty-run sentinel (`no narrative documents found`, `no spec found`, `no code source found`, `no recorded api responses found`, `no tests found`) count as not-yet-ingested. The synthesis surfaces entity and journey names parsed from the cross-cutting bullets (`extract_bolded_names()`) along with per-source counts and links to the underlying files. **Configurable extensions** (per D19): `<workspace>/config.yaml` under `tc-knowledge.documents:` accepts `entity-keywords: [Patient, Provider, Claim]` (case-sensitive additive entity names surfaced wherever they appear in prose, even outside entity-tokened headings) and `journey-headings: [story, flow]` (additive case-insensitive heading tokens widening journey-section detection). Missing keys → empty extension; the helper falls back to the universal core only. **Two methodology docs**: `methodology/project-knowledge.md` (umbrella — three-tier synthesis model, provenance contract, assumptions-vs-facts rule, gap-signal routing, workspace boundaries, Claude judgment layer) and `methodology/learning-from-documents.md` (per-source — covers all five positive dimensions plus the two gap signals with worked examples drawn from the seeded fixture and Claude-judgment-layer paragraphs). **Six templates**: `documentation-model-template.md`, `entities-template.md`, `user-journeys-template.md`, `business-rules-template.md`, `assumptions-template.md`, `system-model-template.md`. **Command page**: `commands/learn-from-docs.md` with Inputs / Outputs / Preconditions / Behavior / Safety / Implementation / Definition of Done / See also. **Workspace template stubs updated**: `entities.md`, `user-journeys.md`, `business-rules.md`, `assumptions.md`, `system-model.md`, `documentation-model.md` now describe multi-source ownership and the `## From <source>` section convention. **SKILL.md updated**: deferral wording removed for `/tc:learn-from-docs`; ships description of the command, the helper's invocation pattern, and the shared synthesizer; per-command judgment-layer focus enumerated. Pre-flight `tests/test_learn_from_docs.py` (26 tests, test-first) covers: uninitialized workspace refused, no-narrative-documents writes a stub documentation-model AND regenerates system-model.md reflecting no-source state, requirements-source files (containing `REQ-\d+`) skipped, seeded-fixture documentation-model lists three source files with line counts, entities extracted with `<path>:<line>` provenance (Account/Workspace/Session at minimum), glossary terms extracted with definitions, user journeys extracted with steps and `path:start-end` provenance, business rules with RFC-2119 modals, assumptions flagged "no direct citation", `undefined-term` (Telemetry) routed to open-questions, `contradictory-rule` (admin-may-delete-vs-must-not-delete) routed to open-questions, `## From documents` section appears in all four cross-cutting artifacts, pre-existing `## From specs` section preserved when `/tc:learn-from-docs` runs, system-model reflects docs ingestion AND lists entity names, byte-identical re-run for documentation-model + cross-cutting + system-model, line-stable open-questions on re-run, `entity-keywords: [Patient, Provider, Claim]` extension surfaces those terms as entities, `journey-headings: [story]` extension surfaces a `## Story` heading as a journey, synthesizer callable standalone. TDD trace: 26 RED on first run; 21 GREEN after first cut of synthesizer + extractor; 5 RED remaining caught five real bugs surfaced as Phase-3 lessons (`_section_range_for` ignored heading level so the H1-journey/H2-Steps fixture missed every step; `_is_generated` treated the helper-written empty-run stub as ingested; synthesizer rendered counts only without entity names; entity-keywords test used `Dashboard` against a lowercase-only fixture; system-model template linked to runtime-only sibling paths via Markdown links that fail the link checker because the template doesn't have those siblings). Fixed each in turn; final 26/26 GREEN. **Cross-link depth pitfall**: methodology files live one directory deeper than SKILL.md (`skills/<name>/methodology/<file>.md`), so `../../../../../docs/...` is the correct depth (five `../`), not the four-level pattern SKILL.md uses. Methodology file's first cut used four levels and shipped six broken links; corrected against the known-good Phase-2 methodology files. **Templates updated to use code spans for runtime-only paths**: `system-model-template.md`'s references to `documentation-model.md`, `spec-derived-model.md`, etc. became `` `documentation-model.md` `` plain code spans (the template lives in `plugins/.../templates/` where those siblings don't exist; the helper still renders real Markdown links in the workspace output). 221-test suite green (195 baseline + 26 new); lint clean; link checker covers 123 files (up from 114 — two methodology docs, six templates, one command page = nine new docs). `verify_skills.py` reports `tc-core PRESENT (phase 1)`, `tc-requirements PRESENT (phase 2)`, `tc-knowledge UNEXPECTED (phase 3) - ahead of schedule` (warn-only; the cap bump lives in Step 3.8). `planning/plan.md` Phase 3 Lessons learned updated with six Step-3.2 entries (heading-level termination; empty-run sentinels; surfacing names not just counts; case-sensitivity in extensions; cross-link depth for nested methodology dirs; templates vs runtime paths). Smoke run against the seeded fixture: three documents parsed, five entities + five terms + one journey (seven steps) + multiple business rules + one assumption extracted with `<path>:<line>` provenance; two gap-signal questions routed to open-questions; cross-cutting `## From documents` sections populated for all four artifacts; `system-model.md` regenerated showing documents as the sole ingested source.
- Step 3.1: `tc-knowledge` skill scaffold + seeded-sample-project fixture. `plugins/test-commander/skills/tc-knowledge/SKILL.md` ships with valid frontmatter (`name: tc-knowledge`, single-line trigger-style `description`), references all five commands, and carries deferral wording for each command paragraph until its own sub-step (3.2–3.6) turns the wording into shipped-behavior description per the "SKILL.md surfaces shipped behavior" convention. Empty `commands/`, `methodology/`, and `templates/` directories (held open by `.gitkeep`) are filled in by Steps 3.2–3.6. New fixture at `tests/fixtures/seeded-sample-project/` carries a deliberately-generic SaaS-dashboard narrative (sign-in, accounts, workspaces, assets — D19-compliant universal vocabulary throughout) across five sub-trees plus a README: `documents/` (three Markdown files — `product-overview.md` narrative including a seeded `undefined-term` and two seeded `contradictory-rule` business rules, `glossary.md` definition-list with five universal SaaS terms, `user-journey-sign-in.md` with ordered steps + edge branches), `specs/openapi.yaml` (six endpoints — `POST /sessions`, `DELETE /sessions/{id}`, `GET /accounts/{id}`, `GET /workspaces` (the seeded `unimplemented-endpoint` — declared in spec but absent from `src/`), `POST /workspaces/{id}/assets` (the seeded `unspecified-status` — no `responses` keys), `GET /workspaces/{id}/assets`; plus six schemas including `AssetUpload` which seeds `schema-without-type`; plus a `bearerAuth` security scheme), `src/` (Python package tree `app/__init__.py`, `app/models/account.py` (`Account` class), `app/models/workspace.py` (`Workspace` class), `app/api/auth.py` (`sign_in`, `sign_out` — docstrings present), `app/api/files.py` (`upload_file` — intentionally lacks a docstring to seed `undocumented-function`; `list_assets` documented), `app/utils/validation.py`, plus `web/app.ts` to seed `language-unsupported-in-v1`), `tests/` (`test_auth.py`, `test_validation.py` — covers `validation.py` and `auth.py` but deliberately not `upload_file`, seeding `untested-function`; plus `web.spec.ts` to seed `unsupported-test-runner`), and `recorded-api/responses.json` (seven playback entries covering every spec endpoint plus the seeded `unspecified-endpoint` (`GET /accounts/me`) and the seeded `mismatched-status` (`POST /workspaces/{id}/assets` returning 500 not declared by spec)). Universal marker convention: `knowledge: <dimension>` embedded in each file's native comment syntax (`<!-- ... -->`, `# ...`, `// ...`, or as the `_knowledge` value string in JSON), discovered by the regex `knowledge:\s*([a-z][a-z0-9-]*)`. Pre-flight `tests/test_tc_knowledge_scaffold.py` (23 tests, test-first) covers: skill directory + SKILL.md presence + valid frontmatter + name=tc-knowledge + body references all five commands; `commands/`, `methodology/`, `templates/` directories present; fixture directory + README documenting marker convention + universal-narrative language; all five fixture sub-trees present; documents/ has three files with seeded definition-list and ordered-step structure; openapi.yaml declares every expected `(method, path)` plus `components`, `schemas`, `securitySchemes`; `src/` has Python modules + classes + functions + at least one docstring + `web/app.ts` for the unsupported-language seed; `tests/` has pytest-shaped files with at least one `test_` function; `recorded-api/responses.json` parses as JSON list of `{method, path, status, ...}` entries; every spec endpoint has a playback entry; all 11 gap-signal markers appear somewhere in the fixture (`undefined-term`, `contradictory-rule`, `unspecified-status`, `schema-without-type`, `unimplemented-endpoint`, `undocumented-function`, `language-unsupported-in-v1`, `unspecified-endpoint`, `mismatched-status`, `untested-function`, `unsupported-test-runner`). TDD trace: 23 RED on first run (before any deliverable existed); 22 GREEN after SKILL.md + fixture authoring; one failure on the gap-signal coverage test because `"_knowledge": "<dim>"` JSON keys produced a `"` between `knowledge` and `:` so the regex missed two seeds; fixed by changing the JSON convention to `"_knowledge": "knowledge: <dim>"` (literal marker phrase as value) and updating the README, then 23/23 GREEN. `pyproject.toml` updated: added `norecursedirs = ["fixtures"]` to `[tool.pytest.ini_options]` because pytest's recursive walk was attempting to collect the fixture's example test files (`tests/fixtures/seeded-sample-project/tests/test_auth.py`, `test_validation.py`) as Test Commander tests and failing on `ModuleNotFoundError: No module named 'app'` — fixtures are example consuming-project assets, not first-party tests. 195-test suite green (172 baseline + 23 scaffold); lint clean; link checker covers 114 files (up from 109 — new SKILL.md, new fixture README, and the Phase 3 plan expansion's anchor references). `verify_skills.py` reports `tc-core PRESENT (phase 1)`, `tc-requirements PRESENT (phase 2)`, `tc-knowledge UNEXPECTED (phase 3) — ahead of schedule` — `UNEXPECTED` is warn-only per the verifier's design (the cap bump to 3 lives in Step 3.8 per the plan). `planning/plan.md` Phase 3 Lessons learned subsection updated with the Step 3.1 entries (in-fixture Python and pytest collection; marker-token uniformity across file types via literal content not container syntax; UNEXPECTED is expected; helper-mirroring evidence starts in 3.2).

### Phase 2 — Requirements and user story intelligence (complete 2026-05-27)

Phase 3 next. `tc-requirements` shipped with all five commands (`/tc:review-requirements`, `/tc:review-user-stories`, `/tc:review-acceptance-criteria`, `/tc:requirements-coverage`, `/tc:requirements-to-tests`) available end to end. Universal-core mechanical rubric (no domain-specific keywords baked in); `tc-requirements.{data-rules,risk,roles-permissions}` extensible via `<workspace>/config.yaml` per D19. Seeded fixture (deliberately-generic SaaS-surface narrative) drives every per-command test. Phase-4-compatible `tc-test-idea/v1` schema contract documented for downstream skills. 172-test suite green; lint clean; 107-file Markdown link check clean. `verify_skills.py` reports `tc-core PRESENT (phase 1)` and `tc-requirements PRESENT (phase 2)` (clean `UNEXPECTED=0`). Two new Per-Phase Conventions codified: Customization-guide audit (D19) and Sub-step lesson capture. Tagged `phase-2` on origin.

#### Added
- Step 2.1: `tc-requirements` skill scaffold + seeded-flawed-requirements fixture. `plugins/test-commander/skills/tc-requirements/SKILL.md` ships with valid frontmatter, lists all five commands, and carries deferral wording for each command paragraph until its own sub-step turns the wording into shipped-behavior description (per the "SKILL.md surfaces shipped behavior" convention). Empty `methodology/` and `templates/` directories (held open by `.gitkeep`) are filled in by Steps 2.2–2.5. The fixture at `tests/fixtures/seeded-flawed-requirements/` carries a deliberately-generic SaaS-surface narrative (auth, forms, reports, sessions, dashboards, notifications) across three Markdown files: `requirements.md` (17 REQ-NNN entries covering all 16 top-level rubric dimensions — clarity, testability, completeness, consistency, atomicity, measurability, ac-quality, edge-cases, negative-cases, data-rules, roles-permissions, nfrs, dependencies, ambiguity, risk, automation-suitability), `user-stories.md` (6 stories, one INVEST violation per letter), and `acceptance-criteria.md` (5 Given/When/Then ACs, one per AC-rubric dimension — missing-edge-cases, missing-negative-cases, untestable-predicate, ambiguous-data-rule, missing-role-context). Every defect is marked with an inline `<!-- defect: <dimension> -->` HTML comment; the fixture loader parses these to drive coverage assertions. Fixture `README.md` documents the convention and how to add a new seeded defect, plus an explicit "domain-specific defects" section directing project teams to extend via config.yaml. Pre-flight `tests/test_tc_requirements_scaffold.py` (15 tests, test-first) covers: skill directory + SKILL.md presence and frontmatter shape, body references all five commands, methodology and templates directories present, fixture directory and four files present, README documents the defect convention, all 16 top-level rubric dimensions seeded across the aggregate fixture, all 6 INVEST letters seeded in `user-stories.md`, all 5 AC-rubric dimensions seeded in `acceptance-criteria.md`, and structural assertions (REQ-NNN identifiers, role-action-benefit shape, Given/When/Then keywords). `verify_skills.py` reports `tc-requirements UNEXPECTED — ahead of schedule` under the current `DEFAULT_PHASE_CAP=1`; the cap bump lives in Step 2.8.
- Phase 2 follow-up (d718b33): ship universal-core rubric defaults; project extensions via config.yaml. Removed inadvertent domain-specificity (e-commerce, PCI) from both the shipped partition-table specification and the test fixture. Step 2.2 partition table for `data-rules`, `risk`, and `roles-permissions` reshaped to ship universal cores only (`{password, secret, token, credential, key}`; `{plain text, plaintext, unencrypted, raw password, hardcoded credential, default password}`; permission verbs `{delete, approve, reject, modify, grant, revoke}` and role qualifiers `{admin, owner, operator}`) with explicit `<workspace>/config.yaml` extension keys under `tc-requirements:` for project-supplied domain vocabulary. Helper unions defaults with extensions at runtime, never replaces. Fixture rewritten to a deliberately-generic SaaS-surface narrative; bookstore narrative dropped. Scaffold test stayed structural and passed without modification.
- Plan and docs backfill: codified new Decision D19 (Test Commander is product-domain-agnostic; consuming projects supply all product-specific knowledge) and added a "generic and product-domain-agnostic" sentence to Product Positioning. Phase 5 BDD tag list reframed as shipped universal classes (`@smoke`, `@regression`, `@manual`, `@exploratory`, `@automated-candidate`) plus project-defined namespaces (`@area:<feature>`, `@risk:<class>`, `@persona:<role>`). Phase 10.5 intent-router and approval-gate illustrative examples, and the full Demo Command Sequence, switched from `checkout` to the universal SaaS-surface example `sign-in`. `docs/chat-command-governance.md` and `docs/controlled-agent-execution.md` updated to match. The d718b33 commit and this follow-up together establish that no specific product domain (e-commerce, healthcare, finance, etc.) is baked into the shipped tool; domain vocabulary enters only through config.yaml extensions, the consuming project's documents, Phase 3 knowledge ingestion, and project-defined tag namespaces.
- Customization guide and Per-Phase Convention: new user-facing doc `docs/user-guide/customizing-for-your-project.md` explains the universal-tool principle and walks through the four extension hooks (config.yaml, project documents, Phase 3 knowledge, tag namespaces) with three worked domain examples (e-commerce, healthcare, research data). README gains a "Universal by Design" section, vision and methodology docs gain a paragraph each, getting-started and workflow user guides gain pointers. `workspace-reference.md` `config.yaml` row updated to call out the extension role. New Per-Phase Convention "Customization-guide audit (per D19)" requires every later phase to update the customization guide whenever it ships a configurable surface — schema additions, new tag namespaces, new keyword sets — with at least one worked example, and to record "no new extensible surface" in the sign-off when no audit is needed. Phase 2 Step 2.7 deliverables, 2.7 DoD, 2.9.2 per-step audit, and 2.9.5 sign-off test all extended to verify the customization guide reflects the Phase 2 `tc-requirements` config.yaml schema. Link checker now covers 94 files; full make verify chain clean.
- Step 2.2: `/tc:review-requirements` end-to-end. `plugins/test-commander/scripts/review_requirements.py` parses every `*.md` in `<workspace>/documents/uploaded/` whose body contains at least one `REQ-\d+` token; emits 16 mechanical checks per the partition table (clarity buzzwords, RFC-2119 modal + vague-predicate testability, ≤10-token completeness, shared-subject + opposing-modal consistency across requirement pairs, comma-list atomicity, qualitative-quantifier measurability, `acceptance criteria` mention without `AC-\d+` pointer, action-modal-without-edge-keyword edge-cases, action-modal-without-failure-keyword negative-cases, universal-core sensitive-data without constraint, universal-core permission-verb without role qualifier, NFR-adjective without numeric threshold, `REQ-\d+` reference-graph dependencies with broken-reference + cycle detection, ambiguity adjectives, universal-core security anti-pattern risk, subjective verb + automation marker automation-suitability); reads `<workspace>/config.yaml` `tc-requirements:` block (tolerant YAML-free parser) and unions project extensions with universal cores (additive, never replacing); writes three artifacts (`requirements-review.md` and `requirements-inventory.md` overwritten byte-deterministically; `open-questions.md` appended with `(req-id, question-text)` dedup so re-runs are line-stable); refuses uninitialized workspace and REQ-ID collisions with exit 2. New methodology doc `requirements-quality-review.md` covers all 16 requirement-level dimensions with definition + mechanical check + worked seeded-fixture example + Claude-judgment-layer paragraph per dimension; explicitly names the three extensible dimensions and how `config.yaml` augments them. New template `requirements-review-template.md` documents the output shape (executive summary, findings table, per-requirement detail, open questions, traceability). New command page `commands/review-requirements.md` follows the per-command-page convention (Inputs / Outputs / Preconditions / Behavior / Safety / Implementation / DoD / See also). `tc-requirements/SKILL.md` updated to describe `/tc:review-requirements`'s shipped behavior and route Claude to the helper (with extension-guide pointer); deferral wording remains only for `/tc:review-user-stories`, `/tc:review-acceptance-criteria`, `/tc:requirements-coverage`, `/tc:requirements-to-tests` (2.3–2.6). Pre-flight `tests/test_review_requirements.py` (10 tests, test-first) covers: uninitialized workspace refused, empty `documents/uploaded/` writes "no requirements found" review, seeded-fixture input writes exactly three artifacts, every one of the 16 partition-table dimensions produces a finding traced to the seeded REQ-ID tagged with that dimension (consistency accepts either REQ-004 or REQ-005), broken-reference REQ-014→REQ-099 emits open question, mutual-exclusion REQ-004/REQ-005 emits open question, idempotent re-run produces byte-identical review and inventory with line-stable open-questions, inventory lists every REQ-ID in document order, REQ-ID collision across two files refused with a clear error naming both files (no artifacts written), config.yaml extension verb `dispense` flags a custom REQ-100 for `roles-permissions` (extension is additive). 121-test suite green; lint clean; link checker covers 97 files. Smoke run against the seeded fixture: 17 requirements parsed, 63 findings across all 16 dimensions, 4 open questions (1 broken reference + 3 detected mutual-exclusion pairs).
- Step 2.3: `/tc:review-user-stories` end-to-end. `plugins/test-commander/scripts/review_user_stories.py` parses every `*.md` in `<workspace>/documents/uploaded/` whose body contains at least one `US-\d+` token; emits 8 mechanical checks (the six INVEST letters: `invest-independent` via explicit dependency clauses `Depends on / After / Requires US-NNN`; `invest-negotiable` via UI coordinates `(x,y)`, pixel dimensions `Npx`, hex colors, or `no deviation` clauses; `invest-valuable` via developer-as-actor patterns or engineering verbs `refactor`/`refactoring`; `invest-estimable` via vague-action words `{better, improved, enhanced, more, faster, smarter, nicer}` without a numeric predicate; `invest-small` via I-want-clause comma-list of ≥ 4 items or > 40-word body; `invest-testable` via subjective-experience words `{feel, delight, delightful, enjoyable, intuitive, satisfying, pleasing, fun, love}`; plus `role-action-benefit` shape (must contain `As a`, `I want`, `So that` case-insensitive); plus `needs-acceptance-criteria` when body lacks `AC-\d+` pointer). Assigns one of three readiness verdicts per story (`ready` / `needs-refinement` / `blocked`) — `blocked` triggers on any shape violation or 3+ INVEST findings. Writes `<workspace>/requirements/user-story-review.md` (overwritten byte-deterministically). Idempotent. New methodology doc `user-story-readiness.md` covers all 8 dimensions with definitions, mechanical checks, worked fixture examples, and Claude judgment-layer paragraphs; documents the verdict table. New template `user-story-review-template.md` documents the output shape. New command page `commands/review-user-stories.md`. `tc-requirements/SKILL.md` updated to ship the command (deferral wording removed for `/tc:review-user-stories`; remains only for `/tc:review-acceptance-criteria`, `/tc:requirements-coverage`, `/tc:requirements-to-tests`). Pre-flight `tests/test_review_user_stories.py` (9 tests, test-first) covers: uninitialized workspace refused, empty `documents/uploaded/` writes "no user stories found" review, seeded fixture writes one artifact (no inventory or open-questions side-files), every one of the 6 INVEST letters produces a finding traced to its seeded US-NNN, all 6 fixture stories flag `needs-acceptance-criteria`, story with an AC pointer does not flag, synthetic story without role-action-benefit shape is flagged, seeded fixture passes the shape check, idempotent re-run byte-identical. 130-test suite green; lint clean; link checker covers 100 files. INVEST checks are universal (English/agile vocabulary only); no new `config.yaml` schema introduced, so the Per-Phase Customization-guide audit records "no new extensible surface" for Step 2.3. Smoke run against the seeded fixture: 6 stories parsed, 13 findings across 7 dimensions (all 6 INVEST letters plus 6 `needs-acceptance-criteria`); all stories verdict `needs-refinement`.
- Step 2.4: `/tc:review-acceptance-criteria` end-to-end. `plugins/test-commander/scripts/review_acceptance_criteria.py` parses every `*.md` in `<workspace>/documents/uploaded/` containing `AC-NNN[-NN]` or `US-NNN` markers; extracts each `AC-NNN[-NN]: Given/When/Then` entry, derives the parent story from the AC ID prefix (`AC-001-01` → `US-001`), and **strips parenthetical asides** from the AC body before applying checks so meta-commentary like `(Happy path only — no edge cases.)` does not falsely satisfy the keyword checks it describes. Emits 5 mechanical AC-rubric checks plus an `orphan` check: `ac-missing-edge-cases` (Given/When/Then body lacks edge keyword), `ac-missing-negative-cases` (lacks failure keyword), `ac-untestable-predicate` (subjective-experience words `{feel, snappy, smooth, fluid, intuitive, delightful, enjoyable, satisfying, pleasing}` or vague predicates `{responsive, fast, slow, quick}` without numeric threshold), `ac-ambiguous-data-rule` (ambiguity words `{appropriately, as needed, suitable, sufficient, sufficiently, properly, accordingly, reasonable, appropriate}`), `ac-missing-role-context` (permission verb without role qualifier; honors `<workspace>/config.yaml` extensions under `tc-requirements.roles-permissions:`), and `orphan` (parent `US-NNN` not in scope). Writes `<workspace>/requirements/acceptance-criteria-review.md` with executive summary, findings grouped by parent story (orphan stories marked inline), a flat findings table, and a traceability footer. Overwritten byte-deterministically. Idempotent. New methodology doc `acceptance-criteria-quality.md` covers all 6 dimensions with definitions, mechanical checks, worked fixture examples, Claude judgment-layer paragraphs, plus a "what disqualifies an AC from being automatable" section. New template `acceptance-criteria-review-template.md`. New command page `commands/review-acceptance-criteria.md`. `tc-requirements/SKILL.md` ships the command (deferral wording removed; remains only for `/tc:requirements-coverage` and `/tc:requirements-to-tests`). `requirements-quality-review.md` now back-links to `acceptance-criteria-quality.md` (the placeholder note "when Step 2.4 lands" removed). Pre-flight `tests/test_review_acceptance_criteria.py` (7 tests, test-first) covers: uninitialized workspace refused, empty `documents/uploaded/` writes "no acceptance criteria found" review, seeded fixture writes one artifact, every one of the 5 AC-rubric dimensions produces a finding traced to its seeded AC-NNN-NN (AC-001-01/02, AC-002-01, AC-003-01, AC-004-01), AC whose parent story is missing is flagged orphan, AC whose parent story exists is not flagged orphan, idempotent re-run byte-identical. TDD trace: 1 failure on first run — three dimensions missed because parenthetical asides in the fixture contained the keywords the checks were meant to detect; fixed by stripping parentheticals before each check. 137-test suite green; lint clean; link checker covers 103 files. Reuses the existing `roles-permissions` config.yaml extension key (no new schema introduced); per-phase customization-guide audit records "extends existing schema, no new keys" for Step 2.4. Smoke run against the seeded fixture: 5 ACs parsed, 6 parent stories in scope, 13 findings across all 5 AC-rubric dimensions (no orphans).
- Step 2.5: `/tc:requirements-coverage` end-to-end. `plugins/test-commander/scripts/requirements_coverage.py` reads `<workspace>/requirements/requirements-inventory.md` (produced by Step 2.2's `/tc:review-requirements`) for the canonical REQ-ID space, scans three downstream surfaces — `test-ideas/*.md` (filename convention `REQ-NNN.md` + body `REQ-\d+` references), `bdd/features/*.feature` (any `REQ-\d+` reference), and `traceability/automation-map.md` (Phase 6 ownership, empty in Phase 2) — and writes two artifacts: `<workspace>/requirements/requirements-coverage.md` (executive summary, full coverage matrix, not-yet-covered list, orphan list, traceability footer) and `<workspace>/traceability/requirements-map.md` (the leaner ID-to-downstream-links map for downstream-skill consumption). Both files overwritten byte-deterministically; idempotent. Refuses with `UninitializedWorkspaceError` when `.test-commander/` is absent and with `InventoryMissingError` when the inventory file is missing or still the unmodified template stub (detected via absence of the Step 2.2 generator markers `Total: **` and `_No requirements parsed yet._`). Orphan detection: any downstream artifact referencing a REQ-ID outside the inventory is flagged with `(target, kind, source)` for resolution. New template `requirements-coverage-template.md` documents both output files. New command page `commands/requirements-coverage.md`. `tc-requirements/SKILL.md` ships the command (deferral wording removed; remains only for `/tc:requirements-to-tests`). Pre-flight `tests/test_requirements_coverage.py` (8 tests, test-first) covers: uninitialized workspace refused; missing-or-stub inventory refused; no-downstream baseline reports `not yet covered` for every parsed REQ; `test-ideas/REQ-NNN.md` links the requirement; orphan test-idea targeting a non-existent REQ flagged; BDD `.feature` containing `REQ-\d+` reference links the requirement; idempotent re-run byte-identical for both coverage file and traceability map; traceability map uses the same REQ-ID space as the inventory. TDD trace: 1 failure on first run — `test_missing_inventory_refused` because the workspace template ships an empty `requirements/requirements-inventory.md` placeholder, so plain file-exists was insufficient; added `_inventory_is_generated()` to require Step 2.2 generator markers; 8/8 pass. 145-test suite green; lint clean; link checker covers 105 files. No new `config.yaml` schema introduced; coverage logic is purely procedural over the ID space. Phase 2 lessons-learned updated with Step 2.5 entry (template-stub-vs-generated detection pattern) per the Per-Phase Sub-step Lesson Capture Convention. Smoke run against the seeded fixture: 17 requirements parsed from the inventory, 0 covered (no downstream artifacts yet — Step 2.6 lands seed test ideas), 0 orphans.
- Step 2.6: `/tc:requirements-to-tests` end-to-end. `plugins/test-commander/scripts/requirements_to_tests.py` consumes `requirements-review.md` (Step 2.2), re-parses `documents/uploaded/*.md` for the full requirement bodies, re-runs Step 2.2's mechanical checks to derive per-REQ `phase_2_findings`, and generates one seed test-idea file per requirement under `<workspace>/test-ideas/<REQ-ID>.md`. **Phase 4 schema contract (`tc-test-idea/v1`)** documented in `commands/requirements-to-tests.md`: stable YAML frontmatter keys `schema`, `requirement_id`, `requirement_title` (derived from first ~12 body words), `source` (originating Markdown path), `status: seed`, `ac_review_present` (bool flag), `phase_2_findings` (the dimensions flagged), `candidates` (three anchor scenarios — happy / edge / negative — each with `id`, `title`, `type`, `source`), `generated_by`. Body has verbatim requirement quote, candidate-scenarios section, optional "Related acceptance-criteria findings" section when AC review is detected, optional Phase-2-findings section, plus notes. **Strict idempotency: existing test-idea files are NEVER overwritten** — Phase 4 (`tc-explore`) enriches them with charters and refined ideas, and those enrichments must survive re-runs; re-running produces `created: 0, skipped: N`. After writing seeds the helper calls `requirements_coverage.coverage()` to refresh `traceability/requirements-map.md` so every REQ row links to its new seed (re-uses Step 2.5's scanner logic). Refuses with `UninitializedWorkspaceError` when `.test-commander/` is absent and with `ReviewMissingError` when `requirements-review.md` is missing or still the unmodified template stub (detected via Step 2.2 generator markers). AC-review detection uses the same template-stub-vs-generated pattern via `_ac_review_is_generated()` (template ships a non-empty placeholder, so `stat().st_size > 0` was insufficient — checks for `## Executive summary` or `no acceptance criteria found` markers). New command page `commands/requirements-to-tests.md` is the Phase 4 schema contract. `tc-requirements/SKILL.md` updated: deferral wording fully removed; SKILL.md now describes all five Phase 2 commands as shipped, with per-command Claude judgment-layer focus. Pre-flight `tests/test_requirements_to_tests.py` (9 tests, test-first) covers: uninitialized workspace refused; missing-or-stub review refused; one seed file per parsed REQ generated; YAML frontmatter contains every stable schema key with correct values; user edits to existing seeds preserved on re-run (`skipped == 17`, file bytes unchanged); idempotent file count; traceability map updated with every REQ → `test-ideas/<REQ-ID>.md` link; AC review presence adds explicit AC-findings reference section to seeds; AC review absence omits the section. TDD trace: 2 failures on first run — (1) `apply_checks` returns `tuple[Findings, OpenQuestions]` not a flat list (unpacked); (2) the workspace template ships a non-empty `acceptance-criteria-review.md` placeholder so `stat().st_size > 0` was insufficient AC-presence detection (added `_ac_review_is_generated()` matching the Step 2.5 lesson). 9/9 pass after fixes. 154-test suite green; lint clean; link checker covers 106 files. **No new `config.yaml` schema introduced** (the schema contract is for the emitted seed files, not for workspace configuration); Per-Phase Customization-guide audit records "no new extensible surface" for Step 2.6. Smoke run against the seeded fixture with all three review artifacts (requirements, ACs) present: 17 seeds created, AC review detected as present (`ac_review_present: true` in every seed), every seed populated with its REQ's actual Phase 2 findings (REQ-001 has `[ambiguity, clarity, edge-cases, negative-cases]`); re-run produces `created: 0, skipped: 17`. Phase 2 lessons-learned updated with Step 2.6 entry per the Sub-step Lesson Capture Convention.
- Step 2.7: dedicated documentation pass. New `docs/user-guide/reviewing-requirements.md` — Phase 2 end-to-end walkthrough mirroring `workflow.md`'s structure (what's available / prerequisites / per-step / what changed on disk / re-running / customizing / beyond / see also). Every example is reproducible against the deliberately-generic seeded fixture; sample command output matches the actual CLI text the helpers print. `docs/command-reference.md` updated: the five Phase 2 commands moved from the "Planned" table to a new "Phase 2 commands (shipped)" section with per-command-page links + methodology + walkthrough references. `docs/workspace-reference.md` enriched: `requirements/` now has a per-file ownership table (command + write mode for each artifact) plus pointers to the walkthrough, methodology docs, and customization guide; the `test-ideas/` section gained a "**`test-ideas/` is seeded by Phase 2**" callout explaining the Phase-4-owner / Phase-2-seeder split and pointing at the `tc-test-idea/v1` schema contract. Status lines refreshed in six places: `README.md` (status header + doc index — now lists both walkthroughs), `docs/install.md` ("Verifying the install" — now references both walkthroughs), `docs/user-guide/getting-started.md` "what's next" (added Phase 2 row), `docs/user-guide/workflow.md` "Beyond Phase 1" (Phase 2 link), `plugins/test-commander/README.md` skill-status table (`tc-core` and `tc-requirements` both marked shipped, removed from "what arrives later"), `docs/user-guide/customizing-for-your-project.md` (forward-looking tense "When Phase 2 Step 2.2 ships" replaced with past tense "Phase 2 ships three extensible rubric dimensions"). Final `tc-requirements/SKILL.md` pass: `grep -i "behavior arrives in\|coming in phase\|until then.*placeholder"` returns no hits; SKILL.md describes every shipped command with no deferral wording, per-command judgment-layer focus enumerated, and routes Claude to each helper via Bash. 154-test suite stayed green; lint clean; link checker covers 107 files (up from 106 — one new doc + its inbound cross-links from README and the user-guide). No code changes in this step; pure documentation. Per the new Per-Phase Sub-step Lesson Capture Convention, `planning/plan.md` Phase 2 Lessons learned subsection updated with the Step 2.7 entries (no bugs; documentation-pass scaffold pattern; status-line-drift checklist).
- Step 2.8: testing finalization. Two deliverables landed. (1) Bumped `DEFAULT_PHASE_CAP` from `1` to `2` in `scripts/verify_skills.py`; verifier now reports `tc-core PRESENT (phase 1)` AND `tc-requirements PRESENT (phase 2)` instead of the previous `tc-requirements UNEXPECTED — ahead of schedule` (warn-only). Clean exit: `PRESENT=2 MISSING=0 MALFORMED=0 UNEXPECTED=0`. (2) New `tests/test_phase_2_integration.py` (1 test, 13 numbered assertion blocks) drives all five Phase 2 helpers in sequence against the seeded fixture: init → customize project.md → /tc:next R3 → upload three fixture files → /tc:review-requirements (asserts all 16 partition-table dimensions fire) → /tc:review-user-stories (asserts all 6 INVEST letters fire) → /tc:review-acceptance-criteria (asserts all 5 AC dimensions fire) → /tc:requirements-coverage pre-seed (17 uncovered, 0 covered, 0 orphans) → /tc:requirements-to-tests (17 seeds created, ac_review_present=true, re-run idempotent 0/17 skipped) → /tc:requirements-coverage post-seed (17 covered, every test-ideas/REQ-NNN.md linked in traceability map) → /tc:next advances past Phase 2 (no longer recommends /tc:review-requirements) → workspace_state confirms phase 2 in_progress → byte-deterministic re-run of overwrite-mode artifacts. Integration smoke landed GREEN on first run; no new bugs surfaced by composition — Phase 2's per-command unit tests caught everything at the unit level. **Coupling fix:** `tests/test_phase_1_signoff.py::test_verify_skills_default_phase_cap_is_1` asserted `DEFAULT_PHASE_CAP == 1` (exact match) at Phase 1 close, which became false when Phase 2 bumped the cap. Loosened the assertion to `>= 1` and renamed to `test_verify_skills_default_phase_cap_at_least_1`. The Phase 1 invariant ("Phase 1 closed by bumping the cap from 0 to 1 or higher") is preserved; the brittle exact-match captured the wrong invariant. Existing verifier tests at `tests/test_verify_skills.py` already passed `phase_cap=` arguments explicitly per test, so they did NOT break. 155-test suite green (154 baseline + 1 integration); lint clean; link checker covers 107 files. `verify_skills.py` reports both shipped skills `PRESENT`. No new config.yaml schema introduced; Per-Phase Customization-guide audit records "no new extensible surface" for Step 2.8. Phase 2 lessons-learned updated with the Step 2.8 entries (prior-phase sign-off coupling on `DEFAULT_PHASE_CAP`; pattern reinforcement: existing verifier tests with explicit `phase_cap=` args did not break; integration-smoke-as-verification pattern; one large flat integration test is easier to read than a tree of factored helpers).

### Phase 1 — Workspace and artifact model (complete 2026-05-26)

Phase 2 next. `tc-core` shipped with all four commands (`/tc:init`, `/tc:status`, `/tc:journal`, `/tc:next`) end to end, with `SKILL.md` routing Claude Code slash-command invocations to the bundled helpers. Workspace template bundled inside the plugin per D18. 96-test suite green; 88-file Markdown link check clean. `verify_skills.py` reports `tc-core PRESENT (phase 1)`. Tagged `phase-1` on origin.

#### Added
- Step 1.1: workspace template — canonical `.test-commander/` workspace template that `/tc:init` copies into consuming projects in Step 1.2. 63 starter files: 35 directories, 33 explicitly-named files from the Workspace Layout (project.md, config.yaml, methodology.md, the 6 requirements files, the 9 product-knowledge files, the 3 traceability maps, the 4 learning files, policy/permissions.yaml + approvals.yaml, audit/actions.jsonl, etc.), plus per-directory README.md placeholders so every empty-by-design directory is reachable through git. Every Markdown starter has an H1 heading and a "Populated by `<command>` (Phase N)" note; YAML starters have the same shape in a `#` comment header; `audit/actions.jsonl` is empty by design (append-only log). Pre-flight `tests/test_workspace_template.py` (7 tests) covers structural completeness, Markdown heading + phase-note conventions, YAML comment-header conventions, and the empty-jsonl invariant.
- Step 1.2: `/tc:init` helper + command file. New Decision D18 — user-facing helpers and templates ship inside the plugin (`plugins/test-commander/scripts/` and `plugins/test-commander/templates/`); dev tooling stays at the repo root. Verified by inspecting the installed plugin cache. The template moved from `templates/workspace/` to `plugins/test-commander/templates/workspace/` so it travels with the plugin. `plugins/test-commander/scripts/init_workspace.py` is the helper: resolves the bundled template relative to its own location; copies into `<target>/.test-commander/`; idempotent (skips existing files, never overwrites); refuses targets that are regular files; supports a `--template` override. `plugins/test-commander/skills/tc-core/commands/init.md` is the per-command page (Inputs / Outputs / Preconditions / Behavior / Safety / Implementation / DoD / See also). Pre-flight `tests/test_init_workspace.py` (4 tests, test-first) covers fresh init, idempotent re-init, partial-existing, and refusal on invalid target. `pyproject.toml` pythonpath extended to include `plugins/test-commander/scripts`. The Phase 0 `test_no_command_behavior_yet` guard in `tests/test_plugin_scaffold.py` was removed (its purpose was to keep Phase 0 from accidentally implementing commands; Phase 1 now legitimately does). Smoke-tested end to end against a tmp dir: 63 created, 63 skipped on re-run, exit 2 on file target.
- Step 1.3: `/tc:status` helper + command file. `plugins/test-commander/scripts/workspace_state.py` produces a deterministic, typed `WorkspaceSnapshot` (workspace path, exists, initialized, last_modified, counts per bucket, populated counts per bucket, phase_status per phase). "Populated" = file bytes differ from the bundled template; orphan workspace files (no template counterpart) also count as populated. Phase status follows `PHASE_OWNERSHIP` — a phase is `in_progress` iff any owned file is populated, else `not_started`. Read-only — never writes. Shared with `/tc:next` in Step 1.5. CLI prints a grep-friendly report. `plugins/test-commander/skills/tc-core/commands/status.md` is the per-command page. Pre-flight `tests/test_workspace_state.py` (6 tests, test-first) covers empty workspace, fresh init, partial user content, full per-phase user content, snapshot determinism, and orphan-file detection. Smoke-tested end to end: 63 files / 0 populated after fresh init, 2 populated after touching project.md + requirements/requirements-inventory.md, phases 1 and 2 flip to in_progress while others stay not_started.
- Step 1.4: `/tc:journal` helper + command file. `plugins/test-commander/scripts/journal.py` ships two modes: `append` (timestamped entry to today's day file at `.test-commander/journal/YYYY-MM-DD.md`) and `summarize` (chronological entries within an inclusive `--from`/`--to` range; AI-generated summaries deferred to Phase 8). Day-file layout: H1 date header plus one H2 timestamp section per entry, written as valid Markdown. Append rejects empty bodies, bodies containing an H2 timestamp heading that would corrupt parsing, naive timestamps, and an uninitialized workspace. Summarize skips `README.md`, returns empty for a missing journal directory, and is read-only. CLI uses argparse subcommands (`append "body"` / `summarize --from YYYY-MM-DD --to YYYY-MM-DD`) with `--target` override. `plugins/test-commander/skills/tc-core/commands/journal.md` is the per-command page (Inputs / Outputs / Preconditions / Behavior / Safety / Out-of-scope / Implementation / DoD). Pre-flight `tests/test_journal.py` (8 tests, test-first) covers the 5 documented cases plus missing-dir summarize, malformed-body refusal, and uninitialized-workspace refusal. Smoke-tested end to end: two appends create a valid day file, summarize prints both in order, empty body exits 2 with a clear error.
- Step 1.5: `/tc:next` heuristics engine. New methodology doc `plugins/test-commander/skills/tc-core/methodology/next-step-inference.md` documents 10 R-rules (R1–R10) with trigger / recommendation / rationale / priority for each. The engine `plugins/test-commander/scripts/next_step.py` reads a `WorkspaceSnapshot` from Step 1.3's `workspace_state.snapshot()`, evaluates every rule, and returns matching `Recommendation(command, explanation, phase, priority)` records sorted by priority ascending. `format_recommendations()` renders the top match as `next: <command>  (Phase N)\n  <explanation>` and the rest under `followups:`. Read-only — never writes anything. Per-command page at `plugins/test-commander/skills/tc-core/commands/next.md` documents the rule table, output shape, and safety contract. Pre-flight `tests/test_next_step.py` (13 tests, test-first) provides one fixture per rule (R1–R10) plus three structural tests: ranked-list ordering, `next:` first line guarantee, and followups format. Smoke-tested end to end across three states: missing workspace → R1; fresh init → R2; after editing `project.md` → R3 with 6 followups. Phase 9 and Phase 10.5 deliberately have no R-rule yet (out-of-scope per methodology doc).
- Step 1.6: documentation pass. `docs/workspace-reference.md` filled in with the canonical `.test-commander/` layout, per-directory purpose, owning-phase notes, commit policy (per D5), and what-lives-outside-the-workspace section (test code at `tests/`, data via fixtures per D6). `docs/command-reference.md` rewritten as an index that links each Phase 1 command directly to its per-command page inside the plugin (`plugins/test-commander/skills/tc-core/commands/<name>.md`) plus a flat table of planned commands by phase. New `docs/user-guide/workflow.md` — end-to-end walkthrough of `/tc:init` → customize metadata → `/tc:status` → `/tc:journal` → `/tc:next` with realistic sample output for each. `tc-core/SKILL.md` rewritten as Claude's entry point for the four shipped Phase 1 commands: each command described, helper invocation pattern documented, per-command pages linked. New Per-Phase Convention "SKILL.md surfaces shipped behavior" codifies this for Phase 2+. README status line updated from "Phase 1 starts next" to "Phase 1 in progress" and the documentation index gained the new workflow page. `docs/install.md` and `docs/user-guide/getting-started.md` "what's next" sections refreshed to reflect that Phase 1 commands are available. Link checker confirms all cross-links resolve (88 files, 0 broken).
- Step 1.7: testing finalization. `scripts/verify_skills.py` CATALOG entry for `tc-core` bumped from phase 0 to phase 1 and `DEFAULT_PHASE_CAP` bumped from 0 to 1, so `make verify` now prints `tc-core PRESENT (phase 1)`. New `tests/test_phase_1_integration.py` (1 test, 9 assertion blocks) drives every Phase 1 helper end-to-end against a fresh tmp consuming project: init (63 created) → re-init (idempotent: 0/63) → fresh-status snapshot (everything not_started) → next (R2 manual customize) → user edits project.md → status reflects in_progress for Phase 1 → next now recommends R3 (`/tc:review-requirements`) with downstream gaps as followups → two journal appends → summarize returns both chronologically → final status keeps Phase 1 in_progress with last_modified set. Complements the per-command unit tests by exercising composition; catches integration regressions that pass when each helper is tested in isolation.
- Step 1.8: Phase 1 sign-off. Cold-user walkthrough of `docs/user-guide/workflow.md` from a clean state (`make uninstall` → `make install` → fresh tmp consuming project → all four helpers in workflow order) succeeded end-to-end; output captured to `/tmp/tc-phase1-walkthrough.log`. Per-step DoD audit confirmed every Step 1.1–1.7 deliverable on disk with passing tests, including the `tc-core/SKILL.md` rewrite landed in 1.6. `planning/plan.md` ## To Do Phase 1 collapsed to a "see Completed" marker; ## Completed section populated with the eight sub-step summaries and the 2026-05-26 date. CHANGELOG Phase 1 heading marked complete with closing summary. README status line refreshed to "Phase 1 complete (2026-05-26); Phase 2 starts next". New `tests/test_phase_1_signoff.py` (12 tests) asserts all 7 Phase 1 test files exist, all 4 helpers present in `plugins/test-commander/scripts/`, all 4 command files under `tc-core/commands/`, methodology doc present, bundled template at the D18 location, `CATALOG["tc-core"]` and `DEFAULT_PHASE_CAP` both equal 1, `tc-core/SKILL.md` describes all four shipped commands and references the bundled helpers with no deferral wording, CHANGELOG/plan markers in place, and pytest count ≥ 84. Annotated `phase-1` tag created and pushed to origin.

### Phase 0 — Repository foundation (complete 2026-05-26)

Repository ready for Phase 1. End-to-end clean install verified: `./bootstrap.sh` → `make install` → `test-commander:tc-core` loaded in Claude Code. 46-test suite green; 23-file Markdown link check clean. Tagged `phase-0` on origin.

#### Added
- Step 0.1: MIT `LICENSE`, expanded `README.md`, `CONTRIBUTING.md`, this `CHANGELOG.md`, and `TODO.md` placeholder.
- Step 0.2: documentation skeleton under `docs/` (vision, architecture, roadmap, methodology, command-reference, workspace-reference, glossary, install) and `docs/user-guide/getting-started.md`. Markdown link checker at `scripts/check_links.py`.
- Step 0.3: Python project foundation. `pyproject.toml` (PDM-managed, `requires-python = ">=3.12"`, ruff and pytest dev deps, tool config inline). `Makefile` with `install`, `lint`, `test`, `build`, `run`, `verify` targets. `docker-compose.yml` placeholder. `.gitignore` for Python/PDM/IDE artifacts. `tests/test_placeholder.py` so pytest exits cleanly before real tests arrive.
- Step 0.4: POSIX `bootstrap.sh` that detects platform (macOS / Linux / WSL / Git Bash); verifies `git`, `make`, Python 3.12, PDM, Docker; auto-installs PDM via the official installer; auto-installs `git` and `make` on Linux/WSL via apt; prints suggested-install hints for the rest and exits non-zero. Idempotent. Never modifies `PATH` or writes a make shim. Bootstrap and `make install` are separate concerns — bootstrap verifies, install installs. Syntax-checked under both bash and dash.
- Step 0.5: plugin scaffold. `.claude-plugin/marketplace.json` declares `test-commander-marketplace` and references the in-repo `test-commander` plugin via `./plugins/test-commander`. `plugins/test-commander/.claude-plugin/plugin.json` declares version `0.0.0` with author, homepage, and repository fields. Plugin gets its own MIT `LICENSE` and a contents-focused `README.md`. `plugins/test-commander/skills/tc-core/SKILL.md` is the descriptor for the `tc-core` umbrella skill — describes `/tc:init`, `/tc:status`, `/tc:journal` and notes that command behavior arrives in Phase 1. Pre-flight test suite `tests/test_plugin_scaffold.py` covers all 8 automated DoD checks (file presence, JSON validity, manifest shape, frontmatter, command references, no-behavior-yet). Validated end-to-end with `claude plugin validate`, `claude plugin marketplace add`, and `claude plugin install`; `tc-core` appears as the sole skill in `claude plugin details test-commander`.
- Step 0.6: skill verifier. `scripts/verify_skills.py` walks `plugins/test-commander/skills/*/SKILL.md`, parses YAML frontmatter, and classifies each skill as PRESENT, MISSING, MALFORMED, or UNEXPECTED. `--phase N` filter restricts the expected set; default cap is 0 (bumped per phase as new skills land). Exit code 0 when every expected skill is PRESENT and nothing MALFORMED; non-zero otherwise; UNEXPECTED warns only. Includes a 20-entry catalog mirroring the TC-Owned Skill Catalog in the plan. Wired into `make verify` between `test` and `check_links`. Pre-flight test suite `tests/test_verify_skills.py` (16 tests) covers the parser, walker, phase filter, exit codes, and a live-run integration test. `pyproject.toml` gets `pythonpath = ["scripts"]` so pytest can import the verifier without sys.path hacks. All five live drills (baseline, --phase 0, --phase 2, MALFORMED corruption, MISSING rename) match expected output and exit codes.
- Step 0.7: `make install` wiring. Decomposed `install` into a five-step chain: `pdm-install` → `validate-manifests` → `marketplace-add` → `plugin-install` → `verify-skills`. Each step is idempotent: `validate-manifests` is read-only; `marketplace-add` and `plugin-install` guard against duplicate registration by checking the CLI lists first. New `make uninstall` target reverses the install and tolerates already-clean state via `-` prefix. `make help` updated. Pre-flight test suite `tests/test_make_install.py` (9 tests) covers target existence, dependency-chain order, the idempotency guards in each step, and `uninstall` tolerance. Documentation updates: `docs/install.md` now lists the five steps accurately and adds an uninstall subsection plus troubleshooting entries for "already registered/installed" and `validate-manifests` aborts; `docs/user-guide/getting-started.md` notes idempotency; plugin README aligned. Final DoD evaluation drill: `make uninstall` → `make install` from clean state (everything ✔), then `make install` again — no errors, exactly one marketplace entry, one plugin entry, `tc-core` PRESENT. Captured to `/tmp/tc-install-fresh.log` and `/tmp/tc-install-rerun.log`.
- Step 0.8: public-skill evaluation pass. `docs/skill-evaluation.md` (86 lines) evaluates the five candidate categories — Mermaid/diagram, devbox/sandbox (Coder, Daytona, Sprites.dev), traceability matrix, accessibility testing, performance testing — by scanning the two locally-installed Claude Code marketplaces (209 plugin entries combined). Each section captures What it does / Why interesting / Decision / Link. All five decisions land at "pass" — no dedicated plugin found in any category. Q1 (a11y/perf out of v1 scope), Q8 (docker-compose first sandbox), and Q12 (build `tc-visualize` ourselves) defaults stand. `chrome-devtools-mcp` noted as a future browser-perf reference. Pre-flight `tests/test_skill_evaluation.py` (4 tests) asserts file existence, line cap, all 5 categories present, and all 4 required fields per section. Cross-linked from `docs/methodology.md` and the README documentation index.
- Step 0.9: Phase 0 sign-off. Cold-user smoke test of `docs/user-guide/getting-started.md` from a clean state (`make uninstall` → `bootstrap.sh` → `make install`) succeeded end to end; output captured to `/tmp/tc-phase0-walkthrough.log`. Per-step DoD audit confirmed every Step 0.1–0.8 deliverable on disk. `planning/plan.md` To Do Phase 0 collapsed to a "see Completed" marker; Completed section populated with the nine sub-step summaries and the 2026-05-26 date. CHANGELOG Phase 0 heading marked complete. `tests/test_phase_0_signoff.py` (5 tests) asserts all five Phase 0 pytest files exist, CHANGELOG and plan are properly marked, and the suite has at least 41 tests. Annotated tag `phase-0` created and pushed to origin.

### Plan and architecture

- New Phase 10.5 (Controlled Agent Execution and Policy-Governed Chat) inserted between Phase 10 and Phase 11. Adds intent router, command planner, permission policy, approval gate, bounded agent execution, output validation, and audit journal.
- Decisions D15 (runtime topology — three patterns, MVP locks to Pattern A) and D16 (frontend users drive workflows, not raw Claude Code) added.
- New Runtime Topology section in `planning/plan.md` defining orchestrator / test runtime / viewer roles and the deployment patterns.
- Phases 10, 11, 12, and 13 updated to route through the Phase 10.5 pipeline.
- New skill `tc-governance` added to the catalog.
- New workspace dirs `.test-commander/policy/` and `.test-commander/audit/`.
- New docs: `docs/controlled-agent-execution.md`, `docs/security-and-permissions.md`, `docs/chat-command-governance.md`, `docs/runtime-approval-flow.md`, `docs/agent-adapters.md`. Updated `docs/architecture.md` and `docs/roadmap.md`.

---

Earlier history precedes the changelog. See `git log` for commits before this file existed.
