"""Test Commander sandboxed environments (Phase 12).

An on-demand, team-accessible Test Commander environment launched from GitHub
Actions. The Phase-10.5 controlled execution pipeline runs inside the sandbox
exactly as it does locally; sandboxing never relaxes governance. Targeting is
safe-by-default (allow-listed domains, blocked private ranges) and provider
credentials stay server-side.
"""
